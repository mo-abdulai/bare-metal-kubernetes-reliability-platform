# OpsPulse PromQL Query Reference

These queries are used by the Phase 6 Grafana dashboard, alert rules, and OpsPulse metrics API. They must be validated against the live Prometheus target set after deployment.

See [`platform/monitoring/README.md`](../../platform/monitoring/README.md) for the active query list and deployment notes.
