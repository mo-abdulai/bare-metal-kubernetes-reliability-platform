# Baseline Evidence

This directory is reserved for sanitized Phase 1 baseline command output.

No sanitized Phase 1 command output files were present in this checkout during Phase 3 documentation work. Future baseline files should avoid kubeconfig credentials, service-account tokens, K3s join tokens, private keys, certificates, passwords, and other secret material.

Suggested evidence files:

- `nodes.txt`
- `system-info.md`
- `cluster-resources.txt`
