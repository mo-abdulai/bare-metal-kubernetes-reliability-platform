apiVersion: v1
kind: Service
metadata:
  name: opspulse-web
  namespace: opspulse
  labels:
    app.kubernetes.io/name: opspulse-web
    app.kubernetes.io/component: frontend
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: opspulse-web
  ports:
    - name: http
      port: 80
      targetPort: http
